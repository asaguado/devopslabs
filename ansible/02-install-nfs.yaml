---

# En la instalación del servidor NFS en local no será necesario añadir un disco

# NFS Server installation (only in master)
- name: NFS Server installation (only in master)
  hosts: master
  gather_facts: false
  vars_files:
    - "group_vars/install-nfs.yaml"
  roles:
    - install-nfs