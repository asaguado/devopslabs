---

# Tareas previas de configuración, estas tareas se han de realizar en todos los nodos.

- name: configuration task
  hosts: all
  gather_facts: false  
  vars_files:
    - "group_vars/configuration-tasks.yaml"
  roles:
    - configuration-tasks